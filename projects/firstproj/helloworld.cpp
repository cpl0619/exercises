#include <iostream>
int main() {
	std::cout << "Hello World" << std::endl;
	// l = left, h = right, k = up, j= down, i = insert in line, o = insert next line.
	// shift + : then w  = save project
	// shift + : then q = quit
	// esc + u = undo, dd = delete current line, yy = copy current line, p = paste,
        std::cout << "code change exercise" << std::endl;

	// l = left, h = right, k = up, j= down, i = insert in line, o = insert next line.
	// l = left, h = right, k = up, j= down, i = insert in line, o = insert next line.
	// l = left, h = right, k = up, j= down, i = insert in line, o = insert next line.
	// l = left, h = right, k = up, j= down, i = insert in line, o = insert next line.
	// l = left, h = right, k = up, j= down, i = insert in line, o = insert next line.
	// l = left, h = right, k = up, j= down, i = insert in line, o = insert next line.
	// l = left, h = right, k = up, j= down, i = insert in line, o = insert next line.
	// l = left, h = right, k = up, j= down, i = insert in line, o = insert next line.
	// l = left, h = right, k = up, j= down, i = insert in line, o = insert next line.
	// l = left, h = right, k = up, j= down, i = insert in line, o = insert next line.
	// l = left, h = right, k = up, j= down, i = insert in line, o = insert next line.
	// l = left, h = right, k = up, j= down, i = insert in line, o = insert next line.
	// l = left, h = right, k = up, j= down, i = insert in line, o = insert next line.
	// l = left, h = right, k = up, j= down, i = insert in line, o = insert next line.
	// shift + : then w  = save project
	// shift + : then q = quit
	// esc + u = undo, dd = delete current line, yy = copy current line, p = paste,
	// shift + : then w  = save project
	// shift + : then q = quit
	// esc + u = undo, dd = delete current line, yy = copy current line, p = paste,
	// shift + : then w  = save project
	// shift + : then q = quit
	// esc + u = undo, dd = delete current line, yy = copy current line, p = paste,
	// shift + : then w  = save project
	// shift + : then q = quit
	// esc + u = undo, dd = delete current line, yy = copy current line, p = paste,
	// shift + : then w  = save project
	// shift + : then q = quit
	// esc + u = undo, dd = delete current line, yy = copy current line, p = paste,
	// shift + : then w  = save project
	// shift + : then q = quit
	// esc + u = undo, dd = delete current line, yy = copy current line, p = paste,
	// shift + : then w  = save project
	// shift + : then q = quit
	// esc + u = undo, dd = delete current line, yy = copy current line, p = paste,
	// shift + : then w  = save project
	// shift + : then q = quit
	// esc + u = undo, dd = delete current line, yy = copy current line, p = paste,
	// shift + : then w  = save project
	// shift + : then q = quit
	// esc + u = undo, dd = delete current line, yy = copy current line, p = paste,
	// shift + : then w  = save project
	// shift + : then q = quit
	// esc + u = undo, dd = delete current line, yy = copy current line, p = paste,
	// shift + : then w  = save project
	// shift + : then q = quit
	// esc + u = undo, dd = delete current line, yy = copy current line, p = paste,
	// shift + : then w  = save project
	// shift + : then q = quit
	// esc + u = undo, dd = delete current line, yy = copy current line, p = paste,
	// shift + : then w  = save project
	// shift + : then q = quit
	// esc + u = undo, dd = delete current line, yy = copy current line, p = paste,
	// shift + : then w  = save project
	// shift + : then q = quit
	// esc + u = undo, dd = delete current line, yy = copy current line, p = paste,
}

	// l = left, h = right, k = up, j= down, i = insert in line, o = insert next line.
	// shift + : then w  = save project
	// shift + : then q = quit
	// esc + u = undo, dd = delete current line, yy = copy current line, p = paste,
